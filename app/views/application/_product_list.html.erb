<p id="notice"><%= notice %></p>

<h5>Products</h5>

<table>
  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.title %></td>
        <td><%= product.price %></td>
        <td><%= product.details %></td>
        <td><%= button_to 'Añadir'%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path %>
